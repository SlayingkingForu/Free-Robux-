# gcc coverage testing tool files

*.gcno
*.gcda
*.gcov
<!DOCTYPE html>
<html>
<head>
    <title>Robuk Working Generator</title>
    <style>
        body {
            background-color: #000000;
            color: #ffffff;
            font-family: Arial, sans-serif;
            text-align: center;
        }

        h1 {
            margin-top: 200px;
        }

        p {
            font-size: 24px;
            margin-top: 50px;
        }

        a {
            color: #ffffff;
            text-decoration: none;
            font-weight: bold;
        }

        #rickroll-video {
            display: none;
        }
    </style>
</head>
<body>
    <h1>Welcome to Robuk Working Generator</h1>
    <p>Click the button below to generate free Robux!</p>
    <a href="#" onclick="playRickroll()">Generate Robux</a>
    <iframe id="rickroll-video" width="560" height="315" src="https://www.youtube.com/embed/dQw4w9WgXcQ?autoplay=1" frameborder="0" allowfullscreen></iframe>

    <script>
        function playRickroll() {
            var video = document.getElementById("rickroll-video");
            video.style.display = "block";
            window.scrollTo(0,document.body.scrollHeight);
        }
    </script>
</body>
</html>
